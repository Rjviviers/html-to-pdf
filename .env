# Defaults for docker-compose

# Mode: api | batch | cli
APP_MODE=cli

# Folders inside the container; do not change unless you know what you're doing
BASE_DIR=/app

# API settings
PORT=8000
WORKERS=1


